---
phase: crakotte-ux-refactor
status: in_progress
last_updated: 2026-05-13T17:00:00.000Z
---

<current_state>
TJM migration commitée (0676185). Reste : fusionner "Projets en attente" dans tab "2. Projets" de RawDataSection + ajouter "Lier à existant".
</current_state>

<completed_work>
- 0676185 — TJM 400€ par défaut pour nouveaux consultants (import-consultants + consultants POST schema)
- Endpoint POST /api/admin/crakotte/set-default-tjm créé (updateMany tjm=null → 400)
- 6acce5a — auto-résolution projets en attente déjà liés en DB (GET pending-projects)
- 3ea1387 — rafraîchissement auto PendingProjectsSection après liaison Données brutes
- 832aaf2 — P2002 silencieux + preview heures ×7.5 + auto-fix backfill
</completed_work>

<remaining_work>
## Action immédiate en prod (avant le code)
Appeler depuis la console browser (prod) :
  fetch('/api/admin/crakotte/set-default-tjm', {method:'POST'}).then(r=>r.json()).then(console.log)
→ Met à jour tous les consultants avec tjm=null à 400€

## Code restant — B+C (non commencé)
Refonte RawDataSection.tsx pour fusionner "Projets en attente" dans tab "2. Projets" :

### Dans RawDataSection.tsx :
1. Ajouter interface PendingProject { id, crakotteProjectId, crakotteProjectName, crakotteCustomerName, suggestedProjet }
2. Ajouter state: pendingProjects + loadPending() (GET /api/admin/crakotte/pending-projects)
3. Appeler loadPending() dans useEffect avec load() + loadStats()
4. Supprimer prop onProjectLinked (plus nécessaire depuis l'extérieur)
5. Ajouter fonctions: approvePending(id, nom, client), ignorePending(id), linkPending(pendingId, crakotteProjectId, existingProjetId, nom)
   - approvePending → POST /api/admin/crakotte/pending-projects/${id}/approve
   - ignorePending → POST /api/admin/crakotte/pending-projects/${id}/ignore
   - linkPending → POST /api/admin/crakotte/import-projects { mode:"link", projectId: crakotteProjectId, existingProjetId }
   - Tous appellent loadPending() + load() après succès
6. Dans tab "2. Projets", AVANT le bouton "Créer tous les inconnus" :
   - Section "À résoudre (N)" si pendingProjects.length > 0
   - Pour chaque pending : carte amber (crakotteProjectName, crakotteCustomerName)
     - Boutons : [Créer] [Ignorer]
     - Si suggestedProjet : bouton "Lier à [nom]" rapide
     - Input recherche combobox (même pattern que canFuse existant) keyed par "pending-{id}" → bouton [Lier]
     - Utilise data?.allDbProjets pour les options (peut être null si data pas encore chargé)

### Supprimer :
- components/admin/crakotte/PendingProjectsSection.tsx
- components/admin/crakotte/CrakotteBottomSections.tsx

### app/admin/crakotte/page.tsx :
- Supprimer import/usage CrakotteBottomSections
- Supprimer fetch pendingProjects dans Promise.all
- Ajouter import RawDataSection
- Remplacer <CrakotteBottomSections initialPending={...} /> par <RawDataSection />
</remaining_work>

<decisions_made>
- TJM par défaut = 400€ pour tous les consultants sans TJM (import Crakotte ET création manuelle)
- "Projets en attente" section standalone → supprimée, intégrée dans tab "2. Projets"
- Clé dropdown pour pending : "pending-{id}" (évite collision avec Crakotte project IDs)
- onProjectLinked prop supprimé (RawDataSection gère son propre state pending)
</decisions_made>

<next_action>
1. En prod : appeler l'endpoint set-default-tjm (voir ci-dessus)
2. Nouvelle session : implémenter B+C dans RawDataSection + cleanup page
</next_action>
