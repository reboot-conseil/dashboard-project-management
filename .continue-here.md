---
phase: crakotte-integration-fixes
task: completed-session
status: paused
last_updated: 2026-05-13T13:31:26.828Z
---

<current_state>
Session de corrections Crakotte terminée. Derniers commits poussés. Deux actions manuelles restantes en production avant que les données apparaissent correctement dans les projets.
</current_state>

<completed_work>

- P2002 (unique constraint) silencieux : les entrées déjà importées sont skippées sans erreur ni crash du sync
- Preview heures × 7.5 : l'aperçu avant import affichait "1h" au lieu des heures réelles — corrigé
- Backfill-orphans auto-fix heures : le bouton "Relier" détecte si MAX(heures) < 2 (signe que les heures n'ont pas été multipliées ×7.5) et corrige automatiquement avant le rattachement

Commits liés (tous poussés) :
- 832aaf2 — fix(crakotte): P2002 silencieux + preview heures ×7.5 + auto-fix heures dans backfill
- 64ab5d9 — fix(crakotte): backfill-orphans — rattache les activités sans projet à leurs projets mappés
- ff8cd57 — fix(crakotte): projets matchedByNom créables + Créer tous inclut les homonymes
- f26eb13 — fix(crakotte): projets en attente marqués APPROVED + dropdown cherchable tri alpha
- 68ac75d — feat(projets): navigation clavier ↑↓ Enter Escape dans les dropdowns de recherche
</completed_work>

<remaining_work>

## Actions manuelles requises en production (pas de code à écrire)

### 1. Cliquer "Relier" dans /admin/crakotte → bandeau stats
- Ce bouton déclenche le endpoint `POST /api/admin/crakotte/backfill-orphans`
- Il va : détecter les heures non corrigées (MAX < 2h) → multiplier par 7.5 → puis rattacher les activités orphelines à leurs projets mappés
- Résultat attendu : les activités avec `projetId: null` obtiennent un `projetId` valide

### 2. Traiter les 19 projets en attente dans /admin/crakotte → "Projets en attente"
- Ces projets Crakotte n'ont aucun match dans le dashboard
- Pour chacun : soit "Créer" (crée un nouveau projet dashboard), soit "Ignorer" (supprime définitivement), soit "Fusionner" (lie à un projet existant)
- Tant qu'un projet reste en attente, ses activités Crakotte ne peuvent pas être rattachées

## Problème visible en production
Les projets dashboard affichent 0 heures / 0 CA parce que les activités Crakotte ont encore `projetId: null` — les 2 actions ci-dessus le résoudront.
</remaining_work>

<decisions_made>

- Correction ×7.5 : faite dans le backfill-orphans côté serveur (pas de migration Prisma nécessaire)
- Critère auto-detect : MAX(heures) < 2 → considéré comme "non corrigé" (heuristique robuste)
- P2002 silencieux : catch sur `Prisma.PrismaClientKnownRequestError` avec code `P2002`, log warning uniquement
- Preview : la conversion heures→jours (÷7.5) était appliquée deux fois dans le calcul d'aperçu — retrait d'une division
</decisions_made>

<blockers>
Aucun bloquant technique. Les 19 projets en attente sont une décision métier (créer ou ignorer), pas un bug.
</blockers>

<context>
L'intégration Crakotte est fonctionnelle mais les données ne remontent pas encore dans les projets dashboard parce que les activités importées sont orphelines (projetId: null). Les corrections de code sont déployées sur Vercel (auto-deploy depuis GitHub). Il ne reste que des actions de configuration côté admin UI.

Architecture Crakotte complète dans : memory/crakotte-integration.md
</context>

<next_action>
Vérifier en production que le bouton "Relier" existe dans le bandeau stats de /admin/crakotte, le cliquer, puis traiter les 19 projets en attente. Aucune modification de code requise.
</next_action>
