# v2.3 — Intégration Microsoft (SSO + Email)
*Créé le 2026-03-11*

## Objectif
1. **Login Microsoft SSO** — connexion via Office 365, restreint aux @reboot-conseil.com
2. **Email automatique** — email de bienvenue envoyé lors de la création d'un compte admin
3. **HTTPS** — certificat auto-signé sur la tour, nginx sert en SSL

## Statut global : ✅ IMPLÉMENTÉ — prêt à déployer

---

## Partie 1 — Azure (prérequis)

### Fait ✅
| Élément | Valeur |
|---------|--------|
| App Registration | `PM Dashboard` |
| Client ID | `200dd327-83d6-4532-abf8-70ef3b232c67` |
| Tenant ID | `74fcce6b-6f2f-4f05-905e-84604db746fa` |
| Redirect URI | `https://192.168.1.63/api/auth/callback/microsoft-entra-id` |
| Client Secret | ⚠️ Exposé en chat — **À RÉGÉNÉRER** après déploiement |

### Reste à faire ⬜
- [x] Ajouter permission `Mail.Send` (Application) dans Azure Portal ✅
- [ ] Cliquer "Grant admin consent for Reboot Conseil" ⚠️ En attente accès admin
- [x] Régénérer le Client Secret ✅ (fait le 2026-03-11)

---

## Partie 2 — Infrastructure (code local, à déployer)

### Fait ✅ (commits locaux, non déployés)
- `infra/nginx.conf` : HTTPS sur port 443, redirection HTTP→HTTPS, montage SSL
- `infra/docker-compose.yml` : ports 80+443, volumes SSL

### Fait sur le serveur ✅
- Certificat SSL : `/home/jon/ssl.crt` et `/home/jon/ssl.key`

### Action requise avant déploiement ⬜
- [ ] Mettre à jour `.env.production` local (voir Partie 4)

---

## Partie 3 — Code (à implémenter)

### À installer
```bash
npm install @auth/microsoft-entra-id
```

### Fichiers à modifier/créer

#### `auth.ts` — Ajouter provider Microsoft
```typescript
import MicrosoftEntraId from "@auth/microsoft-entra-id"

// Dans providers[] :
MicrosoftEntraId({
  clientId: process.env.AUTH_MICROSOFT_ENTRA_ID_ID!,
  clientSecret: process.env.AUTH_MICROSOFT_ENTRA_ID_SECRET!,
  issuer: process.env.AUTH_MICROSOFT_ENTRA_ID_ISSUER!,
}),

// Dans callbacks.signIn :
if (account?.provider === "microsoft-entra-id") {
  const email = user.email ?? ""
  if (!email.endsWith("@reboot-conseil.com")) return false
  const existing = await prisma.consultant.findUnique({ where: { email } })
  if (!existing) {
    await prisma.consultant.create({
      data: { email, nom: user.name ?? email.split("@")[0], role: Role.CONSULTANT, actif: true }
    })
  } else if (!existing.actif) return false
}
return true
```

#### `app/(auth)/login/page.tsx` — Bouton Microsoft
Ajouter avant le formulaire email/password :
```tsx
<form action={async () => { "use server"; await signIn("microsoft-entra-id", { redirectTo: "/" }) }}>
  <button type="submit" className="w-full flex items-center justify-center gap-3 px-4 py-2.5 rounded-lg border border-border bg-card hover:bg-muted transition-colors text-sm font-medium">
    <svg width="18" height="18" viewBox="0 0 23 23">
      <rect x="1" y="1" width="10" height="10" fill="#F25022"/>
      <rect x="12" y="1" width="10" height="10" fill="#7FBA00"/>
      <rect x="1" y="12" width="10" height="10" fill="#00A4EF"/>
      <rect x="12" y="12" width="10" height="10" fill="#FFB900"/>
    </svg>
    Se connecter avec Microsoft
  </button>
</form>
<div className="relative my-4">
  <div className="absolute inset-0 flex items-center"><div className="w-full border-t border-border"/></div>
  <div className="relative flex justify-center text-xs text-muted-foreground"><span className="px-2 bg-card">ou</span></div>
</div>
```

#### `lib/email.ts` — Nouveau fichier
```typescript
export async function sendWelcomeEmail(nom: string, email: string, password: string) {
  const tenantId = process.env.MICROSOFT_TENANT_ID!
  const clientId = process.env.MICROSOFT_CLIENT_ID!
  const clientSecret = process.env.MICROSOFT_CLIENT_SECRET!
  const from = process.env.EMAIL_FROM!
  const appUrl = process.env.APP_URL ?? "https://192.168.1.63"

  const tokenRes = await fetch(
    `https://login.microsoftonline.com/${tenantId}/oauth2/v2.0/token`,
    {
      method: "POST",
      headers: { "Content-Type": "application/x-www-form-urlencoded" },
      body: new URLSearchParams({
        grant_type: "client_credentials",
        client_id: clientId,
        client_secret: clientSecret,
        scope: "https://graph.microsoft.com/.default",
      }),
    }
  )
  if (!tokenRes.ok) throw new Error("Token Microsoft impossible")
  const { access_token } = await tokenRes.json()

  const mailRes = await fetch(
    `https://graph.microsoft.com/v1.0/users/${from}/sendMail`,
    {
      method: "POST",
      headers: { Authorization: `Bearer ${access_token}`, "Content-Type": "application/json" },
      body: JSON.stringify({
        message: {
          subject: "Votre accès PM Dashboard — Reboot Conseil",
          body: {
            contentType: "HTML",
            content: `<p>Bonjour <strong>${nom}</strong>,</p>
<p>Votre accès à <strong>PM Dashboard</strong> a été créé.</p>
<table>
  <tr><td><strong>URL</strong></td><td><a href="${appUrl}">${appUrl}</a></td></tr>
  <tr><td><strong>Email</strong></td><td>${email}</td></tr>
  <tr><td><strong>Mot de passe</strong></td><td>${password}</td></tr>
</table>
<p>Cordialement, Reboot Conseil</p>`,
          },
          toRecipients: [{ emailAddress: { address: email } }],
        },
        saveToSentItems: false,
      }),
    }
  )
  if (!mailRes.ok) {
    const err = await mailRes.json().catch(() => ({}))
    throw new Error(`Graph API error: ${JSON.stringify(err)}`)
  }
}
```

#### `app/api/admin/users/route.ts` — Déclencher l'email
Après `prisma.consultant.create(...)` dans le cas `action === "create"` :
```typescript
import { sendWelcomeEmail } from "@/lib/email"

// Après la création :
try {
  await sendWelcomeEmail(nom, email, password)
} catch (err) {
  console.error("Email bienvenue non envoyé :", err)
  // Silencieux — le compte existe quand même
}
```

---

## Partie 4 — Variables d'environnement

### `.env.production` local à mettre à jour
Modifier `NEXTAUTH_URL` et ajouter les variables Microsoft :

```env
# Changer http → https
NEXTAUTH_URL=https://192.168.1.63

# Ajouter
AUTH_MICROSOFT_ENTRA_ID_ID=200dd327-83d6-4532-abf8-70ef3b232c67
AUTH_MICROSOFT_ENTRA_ID_SECRET=[NOUVEAU_SECRET_APRES_REGENERATION]
AUTH_MICROSOFT_ENTRA_ID_ISSUER=https://login.microsoftonline.com/74fcce6b-6f2f-4f05-905e-84604db746fa/v2.0
MICROSOFT_TENANT_ID=74fcce6b-6f2f-4f05-905e-84604db746fa
MICROSOFT_CLIENT_ID=200dd327-83d6-4532-abf8-70ef3b232c67
MICROSOFT_CLIENT_SECRET=[NOUVEAU_SECRET_APRES_REGENERATION]
EMAIL_FROM=jonathan.braun@reboot-conseil.com
APP_URL=https://192.168.1.63
```

---

## Partie 5 — Déploiement

Une seule commande depuis le Mac :
```bash
bash infra/deploy.sh
```

Le script gère automatiquement :
1. rsync du code (nginx.conf, docker-compose.yml inclus via `infra/`)
2. Copie de `.env.production` local → serveur
3. Build Docker (avec nouveau port 443)
4. Redémarrage des containers
5. Migration Prisma

---

## Checklist finale avant déploiement

### Azure ⬜
- [ ] Ajouter `Mail.Send` permission + Grant admin consent
- [ ] Régénérer Client Secret

### Code ✅
- [x] Pas de package séparé — provider dans `next-auth/providers/microsoft-entra-id`
- [x] `auth.ts` mis à jour (provider + callbacks + upsert atomique)
- [x] `app/(auth)/login/page.tsx` mis à jour (bouton Microsoft + séparateur)
- [x] `lib/email.ts` créé (Graph API client credentials)
- [x] `app/api/admin/users/route.ts` mis à jour (email silencieux après création)

### Config ✅
- [x] `.env.production` local mis à jour (https + variables Microsoft)

### Déploiement ⬜
- [ ] `bash infra/deploy.sh`
- [ ] Vérifier https://192.168.1.63 accessible
- [ ] Tester login Microsoft
- [ ] Tester création compte → email reçu (après Grant admin consent Azure)
- [ ] Tagger `v2.3.0`
